# Scheduler Tuning Example

This example will optimize bounded scheduler-policy parameters such as process quantum, queue aging, I/O priority weights, CPU affinity, and background-task limits.

Fitness should combine throughput, latency, starvation, context switching, and memory-pressure penalties. Correctness and safety tests remain hard gates.
