# Compiler Optimization Example

This example will explore compiler pass selection and ordering, inlining thresholds, instruction selection, register allocation, code-size versus speed tradeoffs, and memory-bank placement.

Fitness may include execution cycles, binary size, bank crossings, build time, and test correctness. Candidate configurations that fail tests are rejected.
