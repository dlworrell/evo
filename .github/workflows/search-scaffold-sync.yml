name: Structured Search Scaffold Sync

on:
  push:
    branches:
      - agent/evolve-compatible-source-recipes
    paths:
      - 'tests/validate_project_search.py'
      - 'src/project_search*.c'
      - 'src/internal/project_search*.h'
      - 'docs/adr/ADR-0042-structured-recipe-evolution.md'
      - 'docs/engineering/reports/EVO-HRA-014-structured-recipe-evolution-audit.md'
      - 'docs/schemas/evo-project-search-v1.schema.json'
      - 'CMakeLists.txt'
      - 'tests/CMakeLists.txt'
      - 'Makefile.am'
      - '.github/workflows/search-scaffold-sync.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/evolve-compatible-source-recipes
          fetch-depth: 0
      - name: Capture validator output
        run: |
          set +e
          python3 tests/validate_project_search.py > .github/search-validator-diagnostic.txt 2>&1
          status=$?
          cat .github/search-validator-diagnostic.txt
          if [ "$status" -ne 0 ]; then
            git config user.name 'Donovan Worrell'
            git config user.email 'donovan.worrell@just-a-geek.com'
            git add .github/search-validator-diagnostic.txt
            git commit -m 'diagnostic: capture structured search validator failure'
            git push origin HEAD:agent/evolve-compatible-source-recipes
          fi
          exit "$status"
